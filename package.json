{
  "name": "geeky-chef",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && nodemon\" \"cd frontend && npm start\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
